spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  application:
    name: smart-home
  profiles:
    active: local
  # DataSource Config
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 123456
    url: jdbc:mysql://localhost:3306/smart-home?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
